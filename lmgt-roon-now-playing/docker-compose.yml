services:
  app_proxy:
   environment:
     APP_HOST: ${APP_ID}_roonnp_1
     APP_PORT: 3000

  roonnp:
    image: ghcr.io/arthursoares/roon-now-playing:latest
    restart: unless-stopped
    ports:
      - "3000:3000" #port change
    volumes:
      - ./config:/app/config
      - ./cache:/app/cache
    environment:
      - PORT=3000